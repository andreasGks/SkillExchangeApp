<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Required for Maps API key -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:name=".beforelogin.login.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/exskills_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/exskills_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkillExchangeApp"
        tools:targetApi="31">

        <!-- Map API key metadata -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBLUFM45Z5b-voxnuK8QGFgmdPCpJr6IU4"/>

        <!-- MainActivity declaration -->
        <activity
            android:name=".beforelogin.login.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SkillExchangeApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AfterLoginActivity -->
        <activity
            android:name=".afterlogin.AfterLoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SkillExchangeApp" />

        <!-- FullScreenImageActivity declaration -->
        <activity
            android:name=".afterlogin.FullScreenImageActivity"
            android:exported="true"
            android:label="@string/full_screen_image_activity"
            android:theme="@style/Theme.SkillExchangeApp" />

        <activity android:name=".afterlogin.profilescreen.editprofilescreen.MapPickerActivity" />

        <!-- ChatActivity addition -->
        <activity android:name=".afterlogin.chat.ChatActivity"
            android:exported="true" />

    </application>

</manifest>
